---
phase: 07-performance-benchmarking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/benchmark_dims.rs
autonomous: true

must_haves:
  truths:
    - "Benchmark runs at multiple (DIM, SEQ) configurations"
    - "Compile time, eval time, and reload time measured per configuration"
    - "Throughput (steps/sec) reported per configuration"
    - "Results include DIM=768 SEQ=256 (target configuration)"
  artifacts:
    - path: "examples/benchmark_dims.rs"
      provides: "Multi-configuration benchmark with timing report"
      min_lines: 100
---

<objective>
Benchmark training throughput across multiple (DIM, SEQ) configurations including the target DIM=768 SEQ=256.

Purpose: PERF-01 requires benchmark at DIM=768 SEQ=256. PERF-03 requires documenting the impact of sequence length and channel dimension. This benchmark sweeps configurations and reports timing.

Output: `examples/benchmark_dims.rs` — multi-config benchmark
</objective>

<execution_context>
@/Users/nat/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nat/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-configuration benchmark</name>
  <files>examples/benchmark_dims.rs</files>
  <action>
Create `examples/benchmark_dims.rs` that benchmarks training across configurations.

Configurations to test:
- (32, 16), (64, 16), (64, 32), (128, 16), (128, 32)
- (256, 64), (512, 128), (768, 256) — target config

For each config:
1. Compile 1-layer conv1x1 model
2. Run 5 training steps (forward + reload)
3. Measure: compile time, avg forward time, avg reload time
4. Report throughput (steps/sec)

Print a summary table at the end.
Note: Large configs (768, 256) may fail to compile on ANE — catch and report errors.

Print "OK benchmark_dims" on success.
  </action>
  <verify>
Build: `cargo build --example benchmark_dims 2>&1 | grep "^error"`
  </verify>
  <done>
benchmark_dims.rs compiles. Tests multiple DIM/SEQ configurations with timing.
</done>
</task>

</tasks>

<verification>
1. `cargo build --example benchmark_dims` succeeds
2. Tests multiple configurations including target DIM=768 SEQ=256
3. Measures compile, forward, reload times per config
4. Reports throughput summary table
5. Handles ANE compile failures gracefully for large configs
</verification>

<success_criteria>
- PERF-01: Benchmark at DIM=768 SEQ=256 (attempted, result reported)
- PERF-02: Timing breakdown per configuration
- PERF-03: DIM and SEQ tuning results documented
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-benchmarking/07-01-SUMMARY.md`
</output>
