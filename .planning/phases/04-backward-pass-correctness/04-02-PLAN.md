---
phase: 04-backward-pass-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mil/programs.rs
  - examples/test_backward_qkv.rs
autonomous: true

must_haves:
  truths:
    - "bwd_qkv_mil() generates valid ANE MIL using only verified ops (conv, add, slice_by_size)"
    - "QKV backward program compiles on ANE without errors"
    - "QKV backward receives packed (dq, dk, dv) input and uses slice_by_size to unpack"
    - "QKV backward computes dx = conv(Wqt, dq) + conv(Wkt, dk) + conv(Wvt, dv)"
  artifacts:
    - path: "src/mil/programs.rs"
      provides: "bwd_qkv_mil() function generating ANE-compatible QKV backward MIL"
      min_lines: 40
    - path: "examples/test_backward_qkv.rs"
      provides: "QKV backward compile + eval test"
      min_lines: 40
  key_links:
    - from: "examples/test_backward_qkv.rs"
      to: "src/mil/programs.rs"
      via: "bwd_qkv_mil() call"
      pattern: "bwd_qkv_mil"
---

<objective>
Port the QKV backward pass from stories_mil.h `gen_qkvb()` to Rust, making it ANE-compatible.

Purpose: The simplest backward generator — just three transposed convolutions summed together. No sub, no sigmoid, no concat needed (output is single dx). This validates the basic backward weight gradient pattern.

Output: `bwd_qkv_mil()` in programs.rs + compile/eval test
</objective>

<execution_context>
@/Users/nat/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nat/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference implementation (stories_mil.h gen_qkvb, lines 149-171):
# Input: [1, 3*DIM, 1, SEQ] — packed (dq, dk, dv) from SDPA backward
# Uses slice_by_size to extract dq, dk, dv
# Loads Wqt, Wkt, Wvt transposed weight matrices via BLOBFILE
# Computes: dxq = conv(Wqt, dq), dxk = conv(Wkt, dk), dxv = conv(Wvt, dv)
# Output: dx = add(dxq, add(dxk, dxv)) — single output, NO concat needed!
#
# This is the SIMPLEST backward pass — only uses conv, add, slice_by_size (all verified).
# No sub decomposition needed. No concat needed. Just 3 convolutions + 2 adds.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bwd_qkv_mil() in programs.rs</name>
  <files>src/mil/programs.rs</files>
  <action>
Port `gen_qkvb()` from stories_mil.h (lines 149-171) to Rust as `bwd_qkv_mil()`.

Function signature: `pub fn bwd_qkv_mil(seq_len: usize, dim: usize) -> String`

Follow the reference code EXACTLY:
- Input: `[1, 3*DIM, 1, SEQ]` fp16 — packed (dq, dk, dv)
- Use slice_by_size to extract:
  - dq: begin=[0,0,0,0], size=[1,DIM,1,SEQ]
  - dk: begin=[0,DIM,0,0], size=[1,DIM,1,SEQ]
  - dv: begin=[0,2*DIM,0,0], size=[1,DIM,1,SEQ]
- Load Wqt, Wkt, Wvt: `[DIM, DIM, 1, 1]` via BLOBFILE (wqt.bin, wkt.bin, wvt.bin)
- CONV_CONST pattern for all conv ops
- dxq = conv(Wqt, dq)
- dxk = conv(Wkt, dk)
- dxv = conv(Wvt, dv)
- dxqk = add(dxq, dxk)
- dx = add(dxqk, dxv)
- Single output: (dx) — NO concat needed!

Use EXACT ANE MIL syntax from stories_mil.h. Also add `bwd_qkv_compile_request()` helper.

Add to public exports in `src/mil/mod.rs`.
  </action>
  <verify>
Run: `cargo build --lib 2>&1 | head -20`
Then: `grep -n "bwd_qkv_mil" src/mil/programs.rs`
  </verify>
  <done>
bwd_qkv_mil() generates valid MIL using only conv, add, slice_by_size, const. No rejected ops. Library compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create QKV backward compile+eval test</name>
  <files>examples/test_backward_qkv.rs</files>
  <action>
Create `examples/test_backward_qkv.rs` following the same pattern as test_backward_ffn.rs.

- Generate MIL via `bwd_qkv_mil(seq_len, dim)`
- Create ANE compile request with weight blobs for Wqt, Wkt, Wvt
- Create input data: pack [dq, dk, dv] into single fp16 tensor [1, 3*DIM, 1, SEQ]
- Use small values (0.01-0.99) to avoid fp16 overflow
- DIM=64, SEQ=16 initially
- Compile on ANE, eval, read output
- Print "OK bwd_qkv compile+eval" on success

Add to Cargo.toml [[bin]] section.
  </action>
  <verify>
Build: `cargo build --example test_backward_qkv 2>&1 | tail -5`
  </verify>
  <done>
test_backward_qkv.rs compiles and is ready for ANE execution.
  </done>
</task>

</tasks>

<verification>
1. `cargo build --lib` succeeds with bwd_qkv_mil() exported
2. `cargo build --example test_backward_qkv` succeeds
3. Generated MIL contains no rejected ops
4. Single output (no concat needed)
</verification>

<success_criteria>
- bwd_qkv_mil() function exists and is publicly exported
- Uses only ANE-verified ops (conv, add, slice_by_size, const)
- Test binary compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-backward-pass-correctness/04-02-SUMMARY.md`
</output>
