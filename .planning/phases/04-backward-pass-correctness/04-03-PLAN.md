---
phase: 04-backward-pass-correctness
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/mil/programs.rs
  - examples/test_backward_sdpa.rs
autonomous: true

must_haves:
  truths:
    - "bwd_sdpa_mil() generates valid ANE MIL using only verified ops (matmul, softmax, reduce_sum, conv, mul, add, slice_by_size, reshape, transpose)"
    - "SDPA backward program compiles on ANE without errors"
    - "SDPA backward uses sub decomposition add(x, mul(y, -1))"
    - "SDPA backward uses multi-output instead of concat"
    - "SDPA backward computes correct dQ, dK, dV gradients from dAttn and saved activations"
  artifacts:
    - path: "src/mil/programs.rs"
      provides: "bwd_sdpa_bwd1_mil() and bwd_sdpa_bwd2_mil() functions"
      min_lines: 120
    - path: "examples/test_backward_sdpa.rs"
      provides: "SDPA backward compile + eval test"
      min_lines: 60
  key_links:
    - from: "examples/test_backward_sdpa.rs"
      to: "src/mil/programs.rs"
      via: "bwd_sdpa_bwd1_mil() and bwd_sdpa_bwd2_mil() calls"
      pattern: "bwd_sdpa"
---

<objective>
Port the SDPA backward pass from stories_mil.h `gen_sdpa_bwd1()` and `gen_sdpa_bwd2()` to Rust, making it ANE-compatible.

Purpose: The most complex backward pass — requires re-computing attention scores (probs) from saved Q, K, V, then computing dV = probs^T @ dAttn, dQ/dK via the softmax backward formula. Split into two MIL programs like the reference. Depends on plan 01 for the multi-output pattern validation.

Output: `bwd_sdpa_bwd1_mil()` and `bwd_sdpa_bwd2_mil()` in programs.rs + compile/eval test
</objective>

<execution_context>
@/Users/nat/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nat/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference implementation (stories_mil.h):
#
# gen_sdpa_bwd1 (lines 174-223):
# Input: [1, 4*DIM, 1, SEQ] — packed (qf, kf, vf, dx2f)
# Extracts qf, kf, vf, dx2f via slice_by_size
# Loads Wot [DIM, DIM, 1, 1] via BLOBFILE
# df = conv(Wot, dx2f)  — backward through output projection
# Reshapes qf, kf, vf, df to [HEADS, HD, SEQ] then transpose to [HEADS, SEQ, HD]
# Recomputes forward attention: scores = Q @ K^T * scale, probs = softmax(scores + mask)
# dV = probs^T @ dAttn  [matmul]
# dP = dAttn @ V  [matmul]
# Reshapes dV, probs, dP back to flat
# Output: concat(dvf, pf, dpf) → CHANGE TO multi-output (dvf, dpf, pf) alphabetical

# gen_sdpa_bwd2 (lines 226-273):
# Input: [1, 2*SCORE_CH + 2*DIM, 1, SEQ] — packed (probs, dp, qf, kf)
# where SCORE_CH = HEADS * SEQ * SEQ (flattened attention scores)
# Extracts probs, dp, qf, kf via slice_by_size
# Reshapes to [HEADS, SEQ, SEQ] and [HEADS, HD, SEQ] formats
# Computes softmax backward:
#   pdp = probs * dp
#   spdp = reduce_sum(pdp, axis=-1, keep_dims=true)
#   dps = sub(dp, spdp) → USE add(dp, mul(spdp, -1)) [sub decomposition]
#   ds = probs * dps * scale
# dQ = ds @ K  [matmul]
# dK = ds^T @ Q  [matmul]  (note: transpose_y=true)
# Reshapes dQ, dK back to [1, DIM, 1, SEQ]
# Output: concat(dqf, dkf) → CHANGE TO multi-output (dkf, dqf) alphabetical

# KEY CONSTRAINTS:
# - matmul IS verified on ANE (between activations, not weights)
# - softmax IS verified on ANE (Phase 3.5 breakthrough)
# - reduce_sum IS verified on ANE (Phase 3.5 breakthrough)
# - reshape, transpose, slice_by_size all verified
# - sub → add(x, mul(y, -1)) decomposition
# - concat → multi-output (alphabetical ordering)
# - SCORE_CH = HEADS * SEQ for flattened [HEADS, 1, SEQ, SEQ] → [HEADS, SEQ*SEQ]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bwd_sdpa_bwd1_mil() in programs.rs</name>
  <files>src/mil/programs.rs</files>
  <action>
Port `gen_sdpa_bwd1()` from stories_mil.h (lines 174-223) to Rust as `bwd_sdpa_bwd1_mil()`.

Function signature: `pub fn bwd_sdpa_bwd1_mil(seq_len: usize, dim: usize, heads: usize, head_dim: usize) -> String`

Follow the reference EXACTLY with these substitutions:
1. `concat` output → multi-output (dvf, dpf, pf) — alphabetical order
2. All other ops (matmul, softmax, reshape, transpose, conv, mul, add, slice_by_size, reduce_sum) are verified

The program:
- Input: [1, 4*DIM, 1, SEQ] fp16 — packed (qf, kf, vf, dx2f)
- slice_by_size to extract each DIM-sized chunk
- Load Wot via BLOBFILE, compute df = conv(Wot, dx2f)
- Reshape qf,kf,vf,df to [HEADS, HD, SEQ], transpose to [HEADS, SEQ, HD]
- Recompute: scores = matmul(Q, K^T) * scale, probs = softmax(scores + mask)
- dV = matmul(probs^T, dAttn)
- dP = matmul(dAttn, V)
- Reshape dV back to [1, DIM, 1, SEQ]
- Reshape probs, dP to [1, HEADS*SEQ, 1, SEQ] (SCORE_CH = HEADS * SEQ)
- Return 3 outputs: (dpf, dvf, pf) alphabetical

Also add `bwd_sdpa_bwd1_compile_request()` helper.
  </action>
  <verify>
Run: `cargo build --lib 2>&1 | head -20`
  </verify>
  <done>
bwd_sdpa_bwd1_mil() generates valid MIL. No sub or concat. Library compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement bwd_sdpa_bwd2_mil() in programs.rs</name>
  <files>src/mil/programs.rs</files>
  <action>
Port `gen_sdpa_bwd2()` from stories_mil.h (lines 226-273) to Rust as `bwd_sdpa_bwd2_mil()`.

Function signature: `pub fn bwd_sdpa_bwd2_mil(seq_len: usize, dim: usize, heads: usize, head_dim: usize) -> String`

Follow the reference EXACTLY with substitutions:
1. `sub(dp, spdp)` → `add(dp, mul(spdp, const(-1.0)))` — sub decomposition
2. `concat(dqf, dkf)` output → multi-output (dkf, dqf) — alphabetical order
3. SCORE_CH = HEADS * SEQ

The program:
- Input: [1, 2*SCORE_CH + 2*DIM, 1, SEQ] fp16 — packed (probs_flat, dp_flat, qf, kf)
- slice_by_size to extract probs_flat (SCORE_CH channels), dp_flat (SCORE_CH channels), qf (DIM channels), kf (DIM channels)
- Reshape probs, dp to [HEADS, SEQ, SEQ]
- Reshape qf, kf to [HEADS, HD, SEQ], transpose to [HEADS, SEQ, HD]
- pdp = mul(probs, dp)
- spdp = reduce_sum(pdp, axes=[-1], keep_dims=true)
- dps = add(dp, mul(spdp, const(-1.0)))  [sub decomposition]
- ds = mul(mul(probs, dps), scale_const)
- dQ = matmul(ds, K)  [transpose_x=false, transpose_y=false]
- dK = matmul(ds, Q)  [transpose_x=true, transpose_y=false]
- Transpose dQ, dK back, reshape to [1, DIM, 1, SEQ]
- Return 2 outputs: (dkf, dqf) alphabetical

Also add `bwd_sdpa_bwd2_compile_request()` helper.
Add all to public exports in mod.rs.
  </action>
  <verify>
Run: `cargo build --lib 2>&1 | head -20`
  </verify>
  <done>
bwd_sdpa_bwd2_mil() generates valid MIL. No sub or concat. Library compiles.
  </done>
</task>

</tasks>

<verification>
1. `cargo build --lib` succeeds with all bwd_sdpa functions exported
2. Generated MIL for bwd1 contains no sub or concat
3. Generated MIL for bwd2 contains no sub or concat (sub decomposed)
4. Both programs use multi-output returns
</verification>

<success_criteria>
- bwd_sdpa_bwd1_mil() and bwd_sdpa_bwd2_mil() exist and are publicly exported
- Use only ANE-verified ops with sub→add+mul decomposition
- Multi-output pattern replaces concat in both programs
</success_criteria>

<output>
After completion, create `.planning/phases/04-backward-pass-correctness/04-03-SUMMARY.md`
</output>
