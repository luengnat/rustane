---
phase: 06-training-loop-integration
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - examples/train_benchmark.rs
autonomous: true

must_haves:
  truths:
    - "ANE forward throughput (steps/sec) measured and reported"
    - "CPU forward throughput measured for comparison"
    - "Speedup ratio calculated and reported"
    - "Compile time, eval time, and reload time individually measured"
  artifacts:
    - path: "examples/train_benchmark.rs"
      provides: "ANE vs CPU throughput benchmark"
      min_lines: 100
---

<objective>
Benchmark ANE training throughput against CPU baseline with timing breakdown.

Purpose: TRL-03 requires ANE speedup demonstration. PERF-01/02 require timing breakdown.

Output: `examples/train_benchmark.rs` — throughput benchmark
</objective>

<execution_context>
@/Users/nat/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/nat/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ANE vs CPU training benchmark</name>
  <files>examples/train_benchmark.rs</files>
  <action>
Create `examples/train_benchmark.rs` that benchmarks ANE vs CPU training.

Structure:
1. Initialize ANE, compile 2-layer model via DeltaCompiler
2. CPU baseline benchmark (20 steps): pure f32 matrix multiply forward
3. ANE benchmark (20 steps): ANE forward + CPU gradient + delta reload
4. Report: CPU throughput, ANE throughput, speedup ratio, per-operation timing
5. Print "OK train_benchmark" on success.
  </action>
  <verify>
Build: `cargo build --example train_benchmark 2>&1 | grep "^error"`
  </verify>
  <done>
train_benchmark.rs compiles. Measures ANE vs CPU throughput with timing breakdown.
</done>
</task>

</tasks>

<verification>
1. `cargo build --example train_benchmark` succeeds
2. CPU forward baseline implemented
3. ANE forward benchmarked
4. Throughput in steps/sec calculated
5. Speedup ratio reported
</verification>

<success_criteria>
- ANE vs CPU throughput comparison (TRL-03)
- Timing breakdown for compile, eval, reload (PERF-02)
</success_criteria>

<output>
After completion, create `.planning/phases/06-training-loop-integration/06-02-SUMMARY.md`
</output>
