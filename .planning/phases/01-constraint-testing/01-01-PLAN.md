---
phase: 1
plan: 1
type: standard
autonomous: true
wave: 1
depends_on: []
---

# Phase 1 Plan 1: Fix MIL_HEADER, Build, Smoke Test

## Objective

Fix the ANE MIL buildInfo brace-doubling bug in `test_ane_constraint.rs`, verify the fix with a smoke test, and confirm the constraint test orchestrator infrastructure works end-to-end.

## Context

@docs/SESSION_ANE_CONSTRAINT_TESTING.md — Root cause analysis and byte-level proof
@examples/test_ane_constraint.rs — Worker binary with 30+ constraint tests (all failing due to MIL_HEADER bug)
@examples/test_ane_constraints.rs — Orchestrator that runs worker as subprocess
@src/mil/programs.rs — Ground truth for correct ANE MIL syntax (lines 213-214)
@docs/ANE_MIL_SYNTAX.md — ANE MIL format reference

## Tasks

### Task 1: Fix MIL_HEADER constant
- **Type**: auto
- **Description**: Change the `MIL_HEADER` const in `examples/test_ane_constraint.rs` (line 145-147) to use correct brace doubling — only the outermost dict wrapper gets `{{`/`}}`, inner pairs use single braces. Copy the exact buildInfo line from `programs.rs` line 214.
- **Verification**: `cargo build --example test_ane_constraint` compiles cleanly
- **Done**: MIL_HEADER produces balanced braces (5 open, 5 close in buildInfo line)

### Task 2: Smoke test with sigmoid
- **Type**: auto
- **Description**: Run `RUSTANE_TEST_NAME=sigmoid_basic cargo run --example test_ane_constraint` to verify the simplest possible ANE program (no weights, single op) compiles and evals successfully.
- **Verification**: Process exits 0, output contains "OK" and sample values
- **Done**: sigmoid_basic produces non-zero, non-NaN output

### Task 3: Smoke test with conv1x1 (weights)
- **Type**: auto
- **Description**: Run `RUSTANE_TEST_NAME=min_surface_ok cargo run --example test_ane_constraint` to verify a weight-bearing program works. This tests conv1x1 with 64-dim, seq=16.
- **Verification**: Process exits 0, output contains "OK" and sample values
- **Done**: conv1x1 with weights produces correct output

### Task 4: Clean up test_minimal.rs
- **Type**: auto
- **Description**: Delete `examples/test_minimal.rs` — it was a debug file used to discover the buildInfo brace issue and is no longer needed.
- **Verification**: File no longer exists
- **Done**: `examples/test_minimal.rs` deleted

## Success Criteria

1. `sigmoid_basic` compiles and evals on ANE (proves MIL syntax is correct)
2. `min_surface_ok` (conv1x1 with weights) compiles and evals (proves weight loading works)
3. No build warnings
4. Test infrastructure ready for Phase 2 full suite run
